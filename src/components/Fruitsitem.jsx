import React from "react";
import {Grid} from "@mui/material"

export const Fruitsitem=()=>{
    return(
     <Grid container spacing={2}>
        <Grid item xs={2}>
            {item}
        </Grid>

     </Grid>
    )
}